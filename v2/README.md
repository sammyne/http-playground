
# Run the server
```bash
cd server 

go run main.go
```

# Run rust-based client
```bash
cd client-rs

cargo run
```

If we enble line 49 in [main.rs](./client-rs/src/main.rs)，the client would crash.